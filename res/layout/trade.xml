<!-- Main Layout of Trade Activity --><LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@android:color/black"
    android:orientation="vertical" >

	<!-- Title TextView-->
	<TextView android:layout_width="fill_parent"
	android:layout_height="wrap_content" android:gravity="center"
	android:textSize="30sp" android:textColor="#f096" android:id="@+id/speedText"
	android:layout_margin="10dp" android:text="@string/titleTrade"/>
	
	<TextView android:layout_width="fill_parent"
	android:layout_height="wrap_content" android:gravity="center"
	android:textSize="20sp" android:textColor="#f096" android:id="@+id/speedText"
	android:layout_margin="10dp" android:text="Test Loc"/>
	
	<!--  Test Button -->
	<Button android:layout_width="fill_parent"
	android:layout_height="wrap_content" android:text="Test Status Pull" android:textColor="#f096"
	android:textSize="30sp" android:id="@+id/buttonStatTest"></Button>
	
</LinearLayout>